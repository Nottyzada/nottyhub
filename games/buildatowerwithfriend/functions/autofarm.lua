local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait();local humanoidRootPart=character:WaitForChild("HumanoidRootPart");local humanoid=character:WaitForChild("Humanoid");local kickStoneRemote=game.ReplicatedStorage:WaitForChild("KickStone");local vim=game:GetService("VirtualInputManager");local TweenService=game:GetService("TweenService");local Module={};Module.Flags={AutoFarm=false,AutoUpgrade={Backpack=false,CutSpeed=false,PickaxeSpeed=false,StoneMultiplier=false,CutterMultiplier=false,PlaceMultiplier=false},Meta={Modo=nil,Valor=50000}};local Theme={Background=Color3.fromRGB(15,12,20),Sidebar=Color3.fromRGB(20,15,25),Accent=Color3.fromRGB(180,130,255),Text=Color3.fromRGB(240,230,255)};local minePosition=Vector3.new(51.139,4.57,-9.209);local globalOrientation=CFrame.Angles(0,math.rad(-90),0);local tolerance=2;local limits=player:WaitForChild("Limits");local mults=player:WaitForChild("Mults");local stones=player:FindFirstChild("Stones") or (player:FindFirstChild("leaderstats") and player.leaderstats:FindFirstChild("Stones"));local bricks=player:FindFirstChild("Bricks") or (player:FindFirstChild("leaderstats") and player.leaderstats:FindFirstChild("Bricks"));local playerTBP=player:WaitForChild("TBP");local UPGRADE_DATA={Backpack={folder=limits,mult=1.5,start=2,price=10,pricemult=1.5},CutSpeed={folder=limits,mult=0.8,start=1.5,price=20,pricemult=1.8},PickaxeSpeed={folder=limits,mult=1.3,start=1,price=20,pricemult=1.6},StoneMultiplier={folder=mults,mult=2,start=1,price=10000,pricemult=3},CutterMultiplier={folder=mults,mult=2,start=1,price=12000,pricemult=3},PlaceMultiplier={folder=mults,mult=2,start=1,price=8000,pricemult=3}};local avisoDado=player:FindFirstChild("AvisoDado") or Instance.new("BoolValue",player);avisoDado.Name="AvisoDado";local avisoGui=nil;function Module:CriarAviso(t,m)if avisoDado.Value then return end;local sg=Instance.new("ScreenGui",player:WaitForChild("PlayerGui"));sg.Name="NottyAvisoGui";sg.ResetOnSpawn=false;avisoGui=sg;local f=Instance.new("Frame",sg);f.Size=UDim2.new(0,280,0,65);f.Position=UDim2.new(1,50,0.85,0);f.BackgroundColor3=Theme.Sidebar;f.BorderSizePixel=0;Instance.new("UIStroke",f).Color=Theme.Accent;local tl=Instance.new("TextLabel",f);tl.Size=UDim2.new(1,-20,0,25);tl.Position=UDim2.new(0,10,0,8);tl.BackgroundTransparency=1;tl.Text=t;tl.TextColor3=Theme.Accent;tl.Font=Enum.Font.GothamBold;tl.TextSize=14;tl.TextXAlignment=0;local ml=Instance.new("TextLabel",f);ml.Size=UDim2.new(1,-20,0,25);ml.Position=UDim2.new(0,10,0,32);ml.BackgroundTransparency=1;ml.Text=m;ml.TextColor3=Theme.Text;ml.Font=Enum.Font.Gotham;ml.TextSize=12;ml.TextXAlignment=0;ml.TextWrapped=true;TweenService:Create(f,TweenInfo.new(0.5,Enum.EasingStyle.Quart,Enum.EasingDirection.Out),{Position=UDim2.new(1,-290,0.85,0)}):Play()end;function Module:FecharAviso()if avisoGui and not avisoDado.Value then avisoDado.Value=true;local f=avisoGui:FindFirstChildOfClass("Frame");if f then local tw=TweenService:Create(f,TweenInfo.new(0.5,Enum.EasingStyle.Quart,Enum.EasingDirection.In),{Position=UDim2.new(1,50,0.85,0)});tw:Play();tw.Completed:Connect(function()avisoGui:Destroy();avisoGui=nil end)end end end;local function setMovement(e)humanoid.WalkSpeed=e and 16 or 0;humanoid.JumpPower=e and 50 or 0;humanoid:SetStateEnabled(3,e)end;local function getUpgradePrice(n)local d=UPGRADE_DATA[n];local v=d.folder:GetAttribute(n) or d.start;local l=math.max(0,math.round(math.log(v/d.start)/math.log(d.mult)));return math.floor(d.price*(d.pricemult^l))end;local function performUpgrade(n)local d=UPGRADE_DATA[n];local p=getUpgradePrice(n);if math.floor(playerTBP.Value)>=p then local u=workspace:WaitForChild("Upgrades"):FindFirstChild(n);if u and u:FindFirstChild("Use") then local pr=u.Use;local op=humanoidRootPart.CFrame;humanoidRootPart.CFrame=u.CFrame*CFrame.new(0,3,0);task.wait(0.3);fireproximityprompt(pr);task.wait(0.3);humanoidRootPart.CFrame=op;return true end end;return false end;local function equipPickaxe()local b=player:FindFirstChild("Backpack");if b then for _,i in pairs(b:GetChildren()) do if i:IsA("Tool") and i.Name:lower():find("pickaxe") then humanoid:EquipTool(i);return i end end end;return character:FindFirstChildWhichIsA("Tool")end;function Module:SetAutoFarm(s)Module.Flags.AutoFarm=s;if s then task.spawn(function()while Module.Flags.AutoFarm do for n,e in pairs(Module.Flags.AutoUpgrade) do if e then performUpgrade(n) end end;local mm=Module.Flags.Meta.Modo;local mv=Module.Flags.Meta.Valor;if mm=="Torre" and math.floor(playerTBP.Value)>=mv then Module:SetAutoFarm(false);break elseif mm=="Coins" and player:FindFirstChild("leaderstats") and player.leaderstats:FindFirstChild("Coins") and player.leaderstats.Coins.Value>=mv then Module:SetAutoFarm(false);break elseif mm=="Tijolos" and player:FindFirstChild("TBP_Placed") and player.TBP_Placed.Value>=mv then Module:SetAutoFarm(false);break end;local tl=math.floor(tonumber(limits:GetAttribute("Backpack")) or 255);local to=math.floor(stones.Value)+math.floor(bricks.Value);if to<tl then humanoidRootPart.CFrame=CFrame.new(minePosition)*globalOrientation;setMovement(false);task.wait(0.5);equipPickaxe();while Module.Flags.AutoFarm and (math.floor(stones.Value)+math.floor(bricks.Value))<tl do if (humanoidRootPart.Position-minePosition).Magnitude>tolerance then humanoidRootPart.CFrame=CFrame.new(minePosition)*globalOrientation end;humanoid.Jump=false;kickStoneRemote:InvokeServer(true);task.wait(0.05);kickStoneRemote:InvokeServer(false);task.wait(0.1)end;setMovement(true)end;if math.floor(stones.Value)>0 then local sf=workspace:FindFirstChild("Saws") and workspace.Saws:FindFirstChild("Saws");local sm=sf and sf:FindFirstChildWhichIsA("Model");if sm and sm:FindFirstChild("Use") and sm.Use:FindFirstChild("UsePP") then local up=sm.Use;local pr=up.UsePP;humanoidRootPart.CFrame=up.CFrame*globalOrientation;setMovement(false);task.wait(0.5);pr:InputHoldBegin();while Module.Flags.AutoFarm and math.floor(stones.Value)>0 do task.wait(0.5);pr:InputHoldBegin();humanoid.Jump=false end;pr:InputHoldEnd();setMovement(true);Module:FecharAviso()end end;if math.floor(bricks.Value)>0 then vim:SendKeyEvent(true,102,false,game);while Module.Flags.AutoFarm and math.floor(bricks.Value)>0 do local bf=workspace:FindFirstChild("Floors") and workspace.Floors:FindFirstChild("Base");local ef=bf and bf:FindFirstChild("Example");local tp=nil;if ef then for _,p in pairs(ef:GetChildren()) do if p:IsA("BasePart") then tp=p;break end end end;if tp then humanoidRootPart.CFrame=tp.CFrame*CFrame.new(0,3,0);local sa=math.floor(bricks.Value);local t=0;while Module.Flags.AutoFarm and math.floor(bricks.Value)==sa and t<0.5 do vim:SendKeyEvent(true,102,false,game);task.wait(0.05);t=t+0.05 end else task.wait(1)end end;vim:SendKeyEvent(false,102,false,game)end;task.wait(1)end end)end end;function Module:SetUpgrade(n,s)if Module.Flags.AutoUpgrade[n]~=nil then Module.Flags.AutoUpgrade[n]=s end end;function Module:SetMeta(m,v)Module.Flags.Meta.Modo=m;Module.Flags.Meta.Valor=v or 50000 end;return Module
